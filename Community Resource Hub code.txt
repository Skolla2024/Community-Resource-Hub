<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Valley City Community Resource Hub</title>
    <!-- Tailwind CSS for styling -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; }
        /* Prevent layout shift before Alpine loads */
        [x-cloak] { display: none !important; }
    </style>
</head>
<body class="bg-slate-50 text-slate-900" x-data="appData()" x-cloak>

    <!-- Navigation -->
    <nav class="bg-slate-800 text-white sticky top-0 z-50 shadow-lg">
        <div class="max-w-7xl mx-auto px-4">
            <div class="flex justify-between h-16 items-center">
                <div class="flex items-center cursor-pointer" @click="currentPage = 'home'">
                    <span class="text-rose-500 font-bold text-2xl mr-2">♥</span>
                    <span class="font-bold text-xl tracking-tight">Valley City Hub</span>
                </div>
                <div class="hidden md:flex space-x-4">
                    <template x-for="item in navItems" :key="item.id">
                        <button @click="currentPage = item.id" 
                                :class="currentPage === item.id ? 'bg-slate-900 text-rose-400' : 'text-slate-300 hover:text-white'"
                                class="px-3 py-2 rounded-md text-sm font-medium transition" x-text="item.label"></button>
                    </template>
                </div>
                <!-- Mobile toggle -->
                <button class="md:hidden" @click="mobileMenu = !mobileMenu">
                    <svg x-show="!mobileMenu" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7" /></svg>
                    <svg x-show="mobileMenu" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" /></svg>
                </button>
            </div>
        </div>
        <!-- Mobile Menu -->
        <div x-show="mobileMenu" class="md:hidden bg-slate-800 border-t border-slate-700 pb-3">
            <template x-for="item in navItems" :key="item.id">
                <button @click="currentPage = item.id; mobileMenu = false" 
                        class="block w-full text-left px-4 py-2 text-slate-300 hover:bg-slate-700" x-text="item.label"></button>
            </template>
        </div>
    </nav>

    <!-- Page Content -->
    <main>
        <!-- Page: HOME -->
        <template x-if="currentPage === 'home'">
            <div>
                <!-- Hero -->
                <div class="bg-slate-800 text-white py-20 px-4 text-center">
                    <h1 class="text-4xl md:text-5xl font-extrabold mb-4">Connecting Valley City</h1>
                    <p class="text-slate-300 max-w-2xl mx-auto mb-8 text-lg">Your central hub for community resources, non-profits, and support services.</p>
                    <button @click="currentPage = 'directory'" class="bg-rose-600 hover:bg-rose-700 px-8 py-3 rounded-lg font-bold shadow-lg transition">Find Help Now</button>
                </div>

                <!-- Highlights Section -->
                <section class="max-w-7xl mx-auto px-4 py-16">
                    <h2 class="text-3xl font-bold text-center mb-12">Community Spotlights</h2>
                    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                        <template x-for="res in resources.filter(r => r.highlight)" :key="res.id">
                            <div class="bg-white rounded-xl shadow border border-slate-100 overflow-hidden">
                                <div :class="res.color" class="h-32 flex items-center justify-center text-slate-400 font-bold italic">Resource Image</div>
                                <div class="p-6">
                                    <span class="text-xs font-bold text-rose-600 uppercase" x-text="res.category"></span>
                                    <h3 class="text-xl font-bold mt-1 mb-2" x-text="res.name"></h3>
                                    <p class="text-slate-600 text-sm mb-4" x-text="res.description"></p>
                                    <button @click="currentPage = 'directory'" class="text-rose-600 font-semibold hover:underline">Details →</button>
                                </div>
                            </div>
                        </template>
                    </div>
                </section>
            </div>
        </template>

        <!-- Page: DIRECTORY -->
        <template x-if="currentPage === 'directory'">
            <div class="max-w-7xl mx-auto px-4 py-12">
                <div class="text-center mb-10">
                    <h2 class="text-3xl font-bold">Resource Directory</h2>
                    <p class="text-slate-600">Search 2026 local community programs</p>
                </div>

                <!-- Filters -->
                <div class="bg-white p-4 rounded-lg shadow-sm mb-8 flex flex-col md:flex-row gap-4">
                    <input type="text" x-model="search" placeholder="Search by name or service..." class="flex-1 border p-2 rounded-md focus:ring-rose-500 focus:border-rose-500">
                    <select x-model="filter" class="border p-2 rounded-md">
                        <option value="All">All Categories</option>
                        <template x-for="cat in categories" :key="cat">
                            <option :value="cat" x-text="cat"></option>
                        </template>
                    </select>
                </div>

                <!-- Grid -->
                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                    <template x-for="res in filteredResources()" :key="res.id">
                        <div class="bg-white p-6 rounded-lg shadow border border-slate-100">
                            <div class="flex justify-between items-start">
                                <span class="bg-rose-100 text-rose-800 text-xs px-2 py-1 rounded-full font-bold" x-text="res.category"></span>
                                <span x-show="res.highlight" class="text-rose-500">♥</span>
                            </div>
                            <h3 class="text-lg font-bold mt-3" x-text="res.name"></h3>
                            <p class="text-slate-600 text-sm mt-2 mb-4" x-text="res.description"></p>
                            <div class="text-xs text-slate-500 space-y-1">
                                <p x-text="'📍 ' + res.address"></p>
                                <p x-text="'📞 ' + res.phone"></p>
                            </div>
                        </div>
                    </template>
                </div>
            </div>
        </template>

        <!-- Page: SUBMIT -->
        <template x-if="currentPage === 'submit'">
            <div class="max-w-2xl mx-auto px-4 py-12">
                <div class="bg-white rounded-xl shadow-lg p-8">
                    <h2 class="text-2xl font-bold mb-6">Suggest a New Resource</h2>
                    <form @submit.prevent="formSubmitted = true" class="space-y-4">
                        <div x-show="!formSubmitted" class="space-y-4">
                            <div>
                                <label class="block text-sm font-medium mb-1">Organization Name</label>
                                <input required type="text" class="w-full border p-2 rounded">
                            </div>
                            <div>
                                <label class="block text-sm font-medium mb-1">Description</label>
                                <textarea required rows="3" class="w-full border p-2 rounded"></textarea>
                            </div>
                            <button type="submit" class="w-full bg-rose-600 text-white py-3 rounded-lg font-bold">Submit for Review</button>
                        </div>
                        <div x-show="formSubmitted" class="text-center py-10 bg-green-50 rounded">
                            <p class="text-green-800 font-bold text-xl">Thank You!</p>
                            <p class="text-green-700">The resource will be reviewed and added soon.</p>
                            <button type="button" @click="formSubmitted = false" class="mt-4 text-rose-600 font-bold underline">Submit another</button>
                        </div>
                    </form>
                </div>
            </div>
        </template>

        <!-- Page: REFERENCES -->
        <template x-if="currentPage === 'references'">
            <div class="max-w-3xl mx-auto px-4 py-12">
                <div class="bg-white p-8 rounded shadow">
                    <h2 class="text-2xl font-bold mb-6 border-b pb-2">Competition References</h2>
                    <div class="space-y-6">
                        <div>
                            <h3 class="font-bold text-slate-800">Framework Affirmation</h3>
                            <p class="text-sm text-slate-600 mt-1">This site was created using Tailwind CSS and Alpine.js. All code logic and structures were custom-built for this 2026 entry.</p>
                        </div>
                        <div>
                            <h3 class="font-bold text-slate-800">Required Documentation</h3>
                            <ul class="list-disc ml-5 text-sm text-rose-600 mt-1">
                                <li><a href="#">Student Copyright Checklist (PDF)</a></li>
                                <li><a href="#">Work Log (PDF)</a></li>
                            </ul>
                        </div>
                        <p class="text-xs text-slate-400 mt-8">Copyright © 2026 Valley City High School - TSA Webmaster Entry</p>
                    </div>
                </div>
            </div>
        </template>
    </main>

    <script>
        // Define data function before Alpine initialization
        function appData() {
            return {
                currentPage: 'home',
                mobileMenu: false,
                search: '',
                filter: 'All',
                formSubmitted: false,
                navItems: [
                    { id: 'home', label: 'Home' },
                    { id: 'directory', label: 'Directory' },
                    { id: 'submit', label: 'Submit' },
                    { id: 'references', label: 'References' }
                ],
                categories: ['Food Assistance', 'Health', 'Youth Services', 'Legal Support', 'Education'],
                resources: [
                    { id: 1, name: "Valley Food Pantry", category: "Food Assistance", description: "Providing fresh produce and non-perishable items.", address: "123 Main St", phone: "555-0101", highlight: true, color: "bg-green-100" },
                    { id: 2, name: "Community Health Clinic", category: "Health", description: "Free and low-cost health screenings.", address: "456 Oak Ave", phone: "555-0102", highlight: true, color: "bg-blue-100" },
                    { id: 3, name: "Youth Mentorship Program", category: "Youth Services", description: "After-school tutoring and mentorship.", address: "789 Pine Ln", phone: "555-0103", highlight: true, color: "bg-yellow-100" },
                    { id: 4, name: "Legal Aid Society", category: "Legal Support", description: "Pro bono legal advice for housing.", address: "202 Birch Blvd", phone: "555-0104", highlight: false, color: "bg-slate-100" },
                    { id: 5, name: "Town Library Tech", category: "Education", description: "Basic computer skills workshops.", address: "303 Cedar Ct", phone: "555-0105", highlight: false, color: "bg-purple-100" }
                ],
                filteredResources() {
                    return this.resources.filter(r => {
                        const s = this.search.toLowerCase();
                        const matchesSearch = r.name.toLowerCase().includes(s) || r.description.toLowerCase().includes(s);
                        const matchesFilter = this.filter === 'All' || r.category === this.filter;
                        return matchesSearch && matchesFilter;
                    });
                }
            }
        }
    </script>
    <!-- Load Alpine.js at the end to ensure script functions are ready -->
    <script src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js" defer></script>
</body>
</html>